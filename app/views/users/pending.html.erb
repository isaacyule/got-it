<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <h1>Pending Requests</h1>

      <h2><%= @pending_requests.count %></h2>

      <% @pending_requests.each do |request| %>
        <%= render "request", request: request %>
      <% end %>
    </div>
  </div>
</div>


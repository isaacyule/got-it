<h1>PRODUCT PAGE!!!</h1>
<%= link_to 'home', root_path, class: 'btn btn-primary' %>
<%= link_to 'list product', new_product_path, class: 'btn btn-primary' %>
<div class="container card-container">
  <div class="row">
    <% @products.each do |product| %>
    <div class="col-xs-12 col-sm-6">
      <div class="card" style="background-image: url(<%= product.photo %>)">
        <div class="link-to-product"><%= link_to "", product_path(product), class: 'link-to-product' %></div>
        <div class="card-body">
        </div>
        <div class="card-footer">
          <span class="description"><%= product.name %></span>
          <span class="price">£<%= product.price_per_day %></span>
        </div>
      </div>
    </div>
    <% end %>
  </div>
</div>






